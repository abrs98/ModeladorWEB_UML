import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  cls: {
    fill: 'none',
    stroke: '#0a0500',
    strokeWidth: 1,
  },
  rect: {
    fill: '#0a0500',
    stroke: '#0a0500',
    strokeWidth: 0.5,
  },
}));

export default useStyles;
